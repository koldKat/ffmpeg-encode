# FFmpeg Webapp

Documentation lives in `docs/`.

Documentation maintenance rule:
If a code change affects behavior, UI workflow, API routes, persistence, queue handling, encoding flow, or deployment assumptions, update the relevant file in `docs/` in the same change.

Files:
- `docs/USER-GUIDE.md`
- `docs/TECH.md`
